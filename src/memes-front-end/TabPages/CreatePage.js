import React from 'react';
import { Text, View } from 'react-native';
import { Button } from 'react-native-elements';

export default class CreatePage extends React.Component {
  
    render() {
        return (
            <Text>This is the create meme page</Text>
        );
    }
}

